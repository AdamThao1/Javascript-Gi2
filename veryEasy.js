//Write a function named min that takes two arguments and returns their minimum.

function min(a, b){ //created a function named "min" and gave it two parabales 
if (a < b) {     //if a is less than b then a is returned
    return a;
} else {        //if b is less than a then b is returned
    return b;
}
}